# Database Deployment

## Migrations
Run `dotnet ef database update` in `PEMS.Api` project, pointing to `PEMS.Infrastructure` for migrations.

## Seed Data
Refer to `database/seed/` for base role and permission SQL seeds.
